# Default target for native development
# For cross-compilation, use: cargo build --target aarch64-unknown-linux-gnu
# Or: make cross-build

[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
rustflags = ["-C", "link-arg=-L/usr/lib/aarch64-linux-gnu"]